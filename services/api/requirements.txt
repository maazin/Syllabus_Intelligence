fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pyjwt>=2.8
boto3>=1.34
# Google Calendar OAuth and the sync client (section 13). The routers import it
# lazily inside their handlers, so a missing httpx does not fail startup or the
# health check: it fails the first student who connects a calendar, in
# production, with a 500. It was absent from every requirements file and only
# present locally because another package happened to pull it in.
httpx>=0.27
# Refresh tokens are encrypted at rest (services/api/token_vault.py).
cryptography>=42
python-multipart>=0.0.9
email-validator>=2.1
celery>=5.4
redis>=5.0
sentry-sdk>=2.0
